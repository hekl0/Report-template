---
profile: default

variables:
  {% raw -%}
  report_date: "{{ env('REPORT_DATE', '') }}"
  email_env: "{{ env('EMAIL_ENV', 'prod') }}"
  {%- endraw %}

engine:
  type: spark
  master:
  jobname:

providers:
  source:
    service:
    hostname:
    port:
    path:
    username:
    password: